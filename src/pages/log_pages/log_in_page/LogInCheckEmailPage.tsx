import React from "react";

// Check email page.
import CheckMessage from "../components/check_message";

const LogInCheckEmailPage = () => (
  <CheckMessage message="Check the email to log in" />
);

export default LogInCheckEmailPage;
