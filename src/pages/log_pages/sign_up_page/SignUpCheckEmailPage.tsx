import React from "react";

// Check email page.
import CheckMessage from "../components/check_message";

const SignUpCheckEmailPage = () => (
  <CheckMessage message="Check the email to sign up and complete with username" />
);

export default SignUpCheckEmailPage;
