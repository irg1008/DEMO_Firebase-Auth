import React from "react";

// Check email page.
import CheckMessage from "../components/check_message";

const VerifyEmailPage = () => {
  return <CheckMessage message="Continue to your inbox to verify email and sign" />;
};

export default VerifyEmailPage;
